const { ClientConfig } = require('./clientConfig');
const { VehicleBodyType } = require('./vehicleBodyType');
const { VehicleColor } = require('./vehicleColor');
const { VehicleCondition } = require('./vehicleCondition');
const { VehicleDomain } = require('./vehicleDomain');
const { VehicleFuelType } = require('./vehicleFuelType');
const { VehicleSearchDomain } = require('./vehicleSearchDomain');
const { VehicleStatus } = require('./vehicleStatus');
const { VehicleType } = require('./vehicleType');
const { VehicleWheelDrive } = require('./vehicleWheelDrive');

module.exports = {
  ClientConfig,
  VehicleBodyType,
  VehicleColor,
  VehicleCondition,
  VehicleDomain,
  VehicleFuelType,
  VehicleSearchDomain,
  VehicleStatus,
  VehicleType,
  VehicleWheelDrive,
};
